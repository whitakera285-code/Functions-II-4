Write a function called `findLongestWord` that's given 3 words as parameters and returns the longest word.

For example:

```
var output = `findLongestWord`('a', 'be', 'see');
```

```
console.log(output); // --> 'see'
```

**Note:** If two words have the same word length, return the first word of that length.